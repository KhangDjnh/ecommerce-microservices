version: '4'
services:
  mysql:
    image: mysql:8.0
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: order-service
    ports:
      - "3306:3306"
    volumes:
      - ./mysql:/var/lib/mysql